export const chatRoomName = (roomId: string): string => `chat-room-${roomId}`;

export const travelerRoomName = (travelerId: string): string =>
  `traveler:${travelerId}`;

export const offersRoomName = (): string => 'offers';
