import { Router } from 'express';
// TODO: Import bid controller and middlewares

const router = Router();

// TODO: Implement bid routes
// GET /api/bids - Get bids
// GET /api/bids/:id - Get bid by ID
// POST /api/bids - Create bid (agency)
// POST /api/bids/:id/accept - Accept bid (traveler)

export default router;

