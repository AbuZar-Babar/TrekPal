import { Router } from 'express';
// TODO: Import trip request controller and middlewares

const router = Router();

// TODO: Implement trip request routes
// GET /api/trip-requests - Get trip requests
// GET /api/trip-requests/:id - Get trip request by ID
// POST /api/trip-requests - Create trip request (traveler)
// GET /api/trip-requests/:id/bids - Get bids for trip request

export default router;

