---
description: Reset the database, re-run migrations, and re-seed the platform
---
This workflow is used to wipe the database clean and populate it with fresh seed data. Use this if your local data becomes inconsistent while testing.

// turbo
1. Reset database and re-seed
`npm run db:reset`
