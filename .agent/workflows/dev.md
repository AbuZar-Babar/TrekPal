---
description: Start all three development servers (Backend, Admin, Agency)
---
This workflow allows you to start the backend API and both frontends with a single command.

// turbo
1. Start all development servers
`npm run dev`
